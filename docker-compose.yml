version: "3.9"

services:
  messenger:
    build: .
    container_name: flask_messenger
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
      - ./instance:/app/instance
    restart: unless-stopped
